import install
import uninstall
import build
import upgrade
import update
import init
import publish

var map = {
  'install': install,
  'uninstall': uninstall,
  'build': build,
  'upgrade': upgrade,
  'update': update,
  'init': init,
  'publish': publish,
}
